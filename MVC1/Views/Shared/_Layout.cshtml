<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <header><h1>Logo</h1></header>
    <div>@Html.ActionLink("Home","Index","Home")</div>
    <div>@Html.ActionLink("About","About","Home")</div> 
    <div>@Html.ActionLink("Contact","Contact","Home")</div>
    <div>@Html.ActionLink("Projects","Projects","Home")</div>
    <div>@Html.ActionLink("Check Temp","Fever","Doctor")</div>
    <div>@Html.ActionLink("Random number guessing","RandomNumber","Home")</div>
    <div>
        @RenderBody()
    </div>
    <footer>Rikard Densloe @DateTime.Now.ToString("yyyy-MM-dd")</footer>
</body>
</html>
